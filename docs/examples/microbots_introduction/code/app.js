"use strict";
// app.ts — A simple greeting function with a syntax error
function greet(name) {
    const message = `Hello, ${name}!`;
    console.log(message);
}
+b;
greet("Microbots");
console.log(add(2, 3));
